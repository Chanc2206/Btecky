<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bucket Sort Visualization</title>
</head>
<body>
    <h1>Bucket Sort Visualization</h1>
    <p>Enter numbers to be sorted (comma-separated):</p>
    <input type="text" id="inputNumbers">
    <button onclick="sortNumbers()">Sort</button>
    <p>Sorted Numbers:</p>
    <div id="sortedNumbers"></div>

    <script>
        function sortNumbers() {
            const input = document.getElementById('inputNumbers').value;
            const numbers = input.split(',').map(Number);
            
            // Implement bucket sort algorithm here

            document.getElementById('sortedNumbers').textContent = numbers.join(', ');
        }
    </script>
</body>
</html>

